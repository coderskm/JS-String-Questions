// replace any character
let str = "Hello, How are you";
console.log(str.replace('H','_')) // first occurence of H replaced by _
console.log(str.replace(/H/g, '_')) // replaces each occurrance of H by _
 
